import React from 'react';
import ClaimsList from '../components/ClaimsList';

const ViewClaims = () => {
    return (
        <div>
            <ClaimsList />
        </div>
    );
};

export default ViewClaims;
